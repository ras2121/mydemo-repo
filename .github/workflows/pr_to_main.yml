name: Raise PR to main
on:
  push:
    branches:
      - devops
jobs:
  action:
    name: Action
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Create Pull Request
        uses: peter-evans/create-pull-request@v4
      #   with:
      #     # add-paths: |
      #     #   docs/*
      #     token: ${{ secrets.SECRET_TOKEN }}
      #     branch: devops
      #     branch-suffix: random
      #     # base: main
      #     commit-message: Update workflow
      #     committer: Anup <anupsalunke@hotmail.co.in>
      #     title: '[Example] Update workflow'
      #     body: |
      #       Update workflow
      #       - Updated with *today's* date
      #       - Auto-generated by [create-pull-request][1]
      #     labels: |
      #       automated-pr
      # - name: Create pull request
      #   uses: paygoc6/action-pull-request-another-repo@v1.0.5
      #   env:
      #     API_TOKEN_GITHUB: ${{ secrets.SECRET_TOKEN }}
      #   with:
      #     source_folder: 'docs'
      #     destination_repo: 'ras2121/mydemo-repo'
      #     destination_folder: 'docs'
      #     destination_base_branch: 'main'
      #     destination_head_branch: 'hotfix-${{ github.sha }}'
      #     user_email: 'anupsalunke@hotmail.co.in'
      #     user_name: 'ras2121'